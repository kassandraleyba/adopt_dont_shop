<!DOCTYPE html>
<html>
  <head>
    <title>Adopt, Dont Shop</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <% flash.each do |type, msg| %>
      <div>
        <%= msg %>
      </div>
    <% end %>
    <div class="primary-nav-bar">
      <%= link_to "Pets", "/pets", :class => "primary-nav-buttons" %>
      <%= link_to "Shelters", "/shelters", :class => "primary-nav-buttons" %>
      <%= link_to "Veterinarians", "/veterinarians", :class => "primary-nav-buttons" %>
      <%= link_to "Veterinary Offices", "/veterinary_offices", :class => "primary-nav-buttons" %>
    </div>
    <%= yield %>
  </body>
</html>
